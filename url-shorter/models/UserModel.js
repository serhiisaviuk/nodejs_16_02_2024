export default class UserModel{
    userId;
    name;
    password;

    constructor(userId, name, password) {
        this.userId = userId;
        this.name = name;
        this.password = password;
    }

}
